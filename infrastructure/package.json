{
  "name": "snapmagic-infrastructure",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for SnapMagic MVP",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy": "cdk deploy --profile snap",
    "destroy": "cdk destroy --profile snap",
    "diff": "cdk diff --profile snap",
    "synth": "cdk synth --profile snap"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk": "^2.161.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.161.1",
    "constructs": "^10.0.0"
  }
}
